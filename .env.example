# ─── Turso (required for Vercel / production) ─────────────────────────────────
# Leave unset for local dev — the app defaults to a local SQLite file (dev.db).

# Your Turso database URL (starts with libsql://)
TURSO_DATABASE_URL=

# Your Turso auth token
TURSO_AUTH_TOKEN=
